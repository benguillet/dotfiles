[user]
	name = Benjamin Guillet
	email = benjamin.guillet@gmail.com
	signingkey = 2B8F5E1D
[color]
  ui = true
[format]
	pretty = "Commit:  %C(blue)%H%nAuthor:  %C(green)%aN <%aE>%nDate:    (%C(red)%ar%Creset) %ai%nSubject: %s%n%n%b"
[alias]
	co = checkout
  g = grep --break --heading --line-number
  cp = cherry-pick
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	assume = update-index --assume-unchanged
	unassume = update-index --no-assume-unchanged
	assumed = "!git ls-files -v | grep ^h | cut -c 3-"
  yesterday = !git log --graph --all --since=\"yesterday.midnight\" --until=\"midnight\" --author=\"$(git config user.email)\" --oneline
  today = !git log --graph --all --since=midnight --author=\"$(git config user.email)\" --oneline
  purge-merged = !git branch --merged | tr -d '* ' | grep -v develop | grep -v master | xargs git branch -d
  recent = !git reflog | grep checkout | head -100 | awk '{print $8}' | cat -n | sort -k2 | uniq -f1 | sort -k1 | cut -f2- | head -10
[core]
	editor = vim
	excludesfile = ~/.gitignore
[code]
	editor = vim
[credential]
	helper = osxkeychain
[push]
	default = simple
[heroku]
	account = work
[merge]
	ff = only
[rebase]
	autosquash = true
[commit]
	gpgsign = true
[pull]
	rebase = true
